{
  "name": "creator-growth-os",
  "version": "1.0.0",
  "private": true,
  "description": "Creator Growth OS - MERN stack social platform that helps creators improve their content with AI insights, engagement prediction and a growth dashboard.",
  "scripts": {
    "install:all": "npm install && npm install --prefix server && npm install --prefix client",
    "dev": "concurrently -k -n SERVER,CLIENT -c blue,magenta \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "start": "npm run start --prefix server",
    "build": "npm run build --prefix client"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
